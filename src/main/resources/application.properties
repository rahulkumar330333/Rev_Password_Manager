# Application
spring.application.name=password-manager
server.port=8080

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/password_manager
spring.datasource.username=root
spring.datasource.password=Yoshith@18
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA - THIS IS THE KEY FIX
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Thymeleaf
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# Session
server.servlet.session.timeout=30m

# Encryption
app.encryption.secret=SecurePassManager2024Key!

# Logging
logging.level.com.passmanager=DEBUG
logging.level.org.springframework.security=WARN
spring.jpa.open-in-view=false

# Error details (temporary for debugging)
server.error.include-stacktrace=always
server.error.include-message=always

# Email configuration for OTP (Your project's "Sender" robot account)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=pk20pa419@gmail.com
spring.mail.password=uhvldchdnepvbsxl
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
